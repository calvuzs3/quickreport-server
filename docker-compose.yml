services:
  postgres:
    image: postgres:15
    environment:
      POSTGRES_DB: qreport_db
      POSTGRES_USER: qreport_user
      POSTGRES_PASSWORD: qreport_pass
    ports:
      - "5432:5432"
    volumes:
      - qreport_pg_data:/var/lib/postgresql/data

volumes:
  qreport_pg_data:
